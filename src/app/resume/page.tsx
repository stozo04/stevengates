import { StubPage } from "@/components/stub-page";

export const metadata = { title: "Resume" };

export default function ResumePage() {
  return (
    <StubPage
      title="Resume"
      hint="Single-column dense page with View + Download PDF. Real history: Associa / HOAM Technology (2019→), Halff Associates (2015–2019), Texas Tech BBA in MIS."
      phase="D"
    />
  );
}
