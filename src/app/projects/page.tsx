import { StubPage } from "@/components/stub-page";

export const metadata = { title: "Projects" };

export default function ProjectsPage() {
  return (
    <StubPage
      title="Projects"
      hint="The full grid: Kayley_Cowork, Loan Tracker MCP, MilaGates, Gatesflix, OpenClaw — each with a real case study, real metrics, real screenshots."
      phase="C"
    />
  );
}
