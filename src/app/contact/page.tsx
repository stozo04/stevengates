import { StubPage } from "@/components/stub-page";

export const metadata = { title: "Contact" };

export default function ContactPage() {
  return (
    <StubPage
      title="Contact"
      hint="Email, GitHub, LinkedIn, X — the direct routes. No contact form theater."
      phase="D"
    />
  );
}
